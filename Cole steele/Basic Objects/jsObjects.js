var person ={
    name: 'seyi',
    age: 21
}

console .log(person.age);
console.log(person['name']);



person.name = 'Ayo';
console.log(person['name']);

person.course = 'Medicine & Surgery'

console.log(person);

//can`t use yourVariable.property if the variable starts with a number.

