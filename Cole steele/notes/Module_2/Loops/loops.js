// Printing 1 - 100
// printing the character in a string

// Printing 1 - 100

// create a count varable = 0
// set the conditon in the while loop (count <= 100)
// console log it out
// increment

var count = 1;

while(count <=100){
    console.log("The count:" + count);
    count++;
}

// printing the character in a string

// create a variable to store the string
// initialize the loop controller (count)
// set the while condition and use the string length (count < string.length)
// console log it out 
// increment

var myString = "Seyifunmi";
var i = 0; // count
var stringLength = myString.length;

while(i < stringLength){
    console.log(myString[i]);
    i++;
}